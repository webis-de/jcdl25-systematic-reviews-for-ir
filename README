# Bootstrapping Systematic Reviews in Information Retrieval with LLM-Generated Boolean Queries

This repository contains code for indexing the IR Anthology using Elasticsearch and performing searches with boolean queries.

## Install requirements

```bash
pip3 install -r requirements.txt
```
